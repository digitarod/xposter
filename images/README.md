# images/

添付画像(オプション)を置くフォルダです。

- ここに画像ファイル(png/jpg/gif等)を入れてコミットします
- `schedule.json` の `images` や、ワークフローの `images` 入力で
  `images/ファイル名.png` のように指定します
- 画像を使わない場合はこのフォルダは空のままでOK(テキストのみ投稿されます)
- 1ツイートにつき最大4枚まで
